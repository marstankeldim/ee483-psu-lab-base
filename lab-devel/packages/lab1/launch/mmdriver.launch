<launch>
  <arg name="veh" default="$(env VEHICLE_NAME)"/>

  <!-- Start with an existing detector stack so the launcher has a valid target. -->
  <include file="$(find cmp_ld)/launch/cmp_ld.launch">
    <arg name="veh" value="$(arg veh)"/>
  </include>
</launch>
